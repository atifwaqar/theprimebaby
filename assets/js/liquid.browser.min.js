/* Placeholder:
 * Replace this file with the real LiquidJS browser build:
 * https://github.com/harttle/liquidjs (build a browser bundle)
 * It should expose either `window.liquidjs.Liquid` or `window.Liquid`.
 */ 
window.liquidjs = window.liquidjs || {};
window.liquidjs.Liquid = function() {
  throw new Error("LiquidJS placeholder in use. Please replace assets/js/liquid.browser.min.js with the real LiquidJS browser bundle.");
};
